<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="UniversalSteamMetadata.UniversalSteamMetadataSettingsView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:sys="clr-namespace:System;assembly=mscorlib" xmlns:steam="clr-namespace:Steam" xmlns:steammetadata="clr-namespace:UniversalSteamMetadata">
  <FrameworkElement.Resources>
    <ResourceDictionary>
      <ObjectDataProvider x:Key="BackgroundSourceEnumValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
        <ObjectDataProvider.MethodParameters>
          <x:TypeExtension TypeName="steam:BackgroundSource" />
        </ObjectDataProvider.MethodParameters>
      </ObjectDataProvider>
    </ResourceDictionary>
  </FrameworkElement.Resources>
  <TabControl ScrollViewer.VerticalScrollBarVisibility="Disabled" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
    <TabItem Header="Settings">
      <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="20" Width="Auto">
          <Grid Width="Auto">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
              <RowDefinition />
              <RowDefinition Height="10" />
              <RowDefinition />
            </Grid.RowDefinitions>
            <TextBlock VerticalAlignment="Center" Text="{DynamicResource LOCUniversalSteamMetadataMetadataLanguageLabel}" Margin="0,0,10,0" Grid.Row="0" Grid.Column="0" />
            <ComboBox Name="LanguageSelection" DisplayMemberPath="Value" SelectedValuePath="Key" IsEditable="False" Grid.Row="0" Grid.Column="1" MinWidth="150" SelectedValue="{Binding Settings.LanguageKey}" ItemsSource="{Binding Path=Languages}" />
            <TextBlock Text="{DynamicResource LOCSteamBackgroundSource}" VerticalAlignment="Center" Margin="0,0,10,0" Grid.Row="2" Grid.Column="0" />
            <ComboBox MinWidth="150" Grid.Row="2" Grid.Column="1" SelectedValue="{Binding Settings.BackgroundSource}" ItemsSource="{Binding Source={StaticResource BackgroundSourceEnumValues}}" />
          </Grid>
          <CheckBox Content="{DynamicResource LOCSteamUseVerticalCovers}" Margin="0,12,0,0" IsChecked="{Binding Settings.DownloadVerticalCovers}" />
          <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
            <CheckBox Content="{DynamicResource LOCSteamLimitTagCountLabel}" VerticalAlignment="Center" Margin="0,0,4,0" IsChecked="{Binding Settings.LimitTagsToFixedAmount}" />
            <TextBox Width="50" VerticalAlignment="Center" Text="{Binding FixedTagCountString}" IsEnabled="{Binding Settings.LimitTagsToFixedAmount}" />
          </StackPanel>
          <CheckBox Name="cbTagCategoryPrefix" Content="{DynamicResource LOCSteamSetTagCategoryAsPrefixLabel}" ToolTip="{DynamicResource LOCSteamSetTagCategoryAsPrefixTooltip}" VerticalAlignment="Center" Margin="0,12,4,0" IsChecked="{Binding Settings.SetTagCategoryAsPrefix}" />
          <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
            <FrameworkElement.Style>
              <Style TargetType="{x:Type StackPanel}">
                <Style.Triggers>
                  <DataTrigger Value="True" Binding="{Binding IsChecked, ElementName=cbTagCategoryPrefix}">
                    <Setter Property="IsEnabled" Value="False" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </FrameworkElement.Style>
            <CheckBox Content="{DynamicResource LOCSteamTagPrefixLabel}" VerticalAlignment="Center" Margin="0,0,4,0" IsChecked="{Binding Settings.UseTagPrefix}" />
            <TextBox Width="200" VerticalAlignment="Center" Text="{Binding Settings.TagPrefix}" IsEnabled="{Binding Settings.UseTagPrefix}" />
          </StackPanel>
          <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
            <TextBlock VerticalAlignment="Center" Text="{DynamicResource LOCSteamImportSteamDeckCompatibilityAs}" />
            <ComboBox IsEditable="False" DisplayMemberPath="Name" SelectedValuePath="Field" Margin="6,0" ItemsSource="{Binding SteamDeckCompatibilityFieldOptions}" SelectedValue="{Binding Settings.SteamDeckCompatibilityField}" />
          </StackPanel>
        </StackPanel>
      </ScrollViewer>
    </TabItem>
    <TabItem Header="{DynamicResource LOCSteamTagBlacklistTabLabel}">
      <DockPanel Margin="20">
        <TextBlock TextWrapping="Wrap" HorizontalAlignment="Center" DockPanel.Dock="Top" Text="{DynamicResource LOCSteamTagBlacklistExplanationLabel}" />
        <Grid VerticalAlignment="Stretch" Margin="0,10,0,0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Name="Left" Width="*" />
            <ColumnDefinition Name="Middle" Width="95" />
            <ColumnDefinition Name="Right" Width="*" />
          </Grid.ColumnDefinitions>
          <Grid.RowDefinitions>
            <RowDefinition Name="Header" MaxHeight="20" />
            <RowDefinition Name="Body" />
          </Grid.RowDefinitions>
          <FrameworkElement.Resources>
            <ResourceDictionary>
              <Style x:Key="{x:Type Button}" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="FontSize" Value="40" />
                <Setter Property="Margin" Value="20,12" />
                <Setter Property="Padding" Value="8,2" />
                <Setter Property="FontFamily" Value="{DynamicResource FontIcoFont}" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
              </Style>
            </ResourceDictionary>
          </FrameworkElement.Resources>
          <TextBlock HorizontalAlignment="Center" Grid.Column="0" Grid.Row="0" Text="{DynamicResource LOCSteamTagWhitelistHeader}" />
          <TextBlock HorizontalAlignment="Center" Grid.Column="2" Grid.Row="0" Text="{DynamicResource LOCSteamTagBlacklistHeader}" />
          <ListBox Name="Whitelist" DockPanel.Dock="Top" SelectionMode="Extended" MinHeight="220" VerticalAlignment="Stretch" DisplayMemberPath="Name" Grid.Column="0" Grid.Row="1" ItemsSource="{Binding OkayTags}" />
          <ListBox Name="Blacklist" DockPanel.Dock="Top" SelectionMode="Extended" MinHeight="220" VerticalAlignment="Stretch" DisplayMemberPath="Name" Grid.Column="2" Grid.Row="1" ItemsSource="{Binding BlacklistedTags}" />
          <StackPanel Grid.Column="1" Grid.Row="1" VerticalAlignment="Center">
            <Button Content="" ToolTip="{DynamicResource LOCSteamTagBlacklistTooltip}" Command="{Binding BlacklistCommand}" CommandParameter="{Binding SelectedItems, ElementName=Whitelist}" />
            <Button Content="" ToolTip="{DynamicResource LOCSteamTagWhitelistTooltip}" Command="{Binding WhitelistCommand}" CommandParameter="{Binding SelectedItems, ElementName=Blacklist}" />
          </StackPanel>
        </Grid>
      </DockPanel>
    </TabItem>
  </TabControl>
</UserControl>